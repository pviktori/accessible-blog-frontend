import { useState } from "react";
import Layout from "@/src/components/Layout";

const Home: React.FC = () => {
  return (
    <Layout>
      <h1>Welcome to Blogs Page</h1>
    </Layout>
  );
};

export default Home;
